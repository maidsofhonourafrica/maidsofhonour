/**
 * Note: AppRouter type is exported directly from @project/server
 * Clients should import it like:
 * import type { AppRouter } from '@project/server/src/trpc/router'
 * 
 * This avoids circular dependencies between packages
 */

